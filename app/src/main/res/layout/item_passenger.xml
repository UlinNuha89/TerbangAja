<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_passenger_name_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="13sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/name_label" />

    <TextView
        android:id="@+id/tv_passenger_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="13sp"
        android:layout_marginStart="4dp"
        app:layout_constraintStart_toEndOf="@id/tv_passenger_name_label"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Budi Siregar" />

    <TextView
        android:id="@+id/tv_passenger_id_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        app:layout_constraintStart_toStartOf="@id/tv_passenger_name_label"
        app:layout_constraintTop_toBottomOf="@id/tv_passenger_name_label"
        android:text="@string/id_label" />

    <TextView
        android:id="@+id/tv_passenger_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:layout_marginStart="4dp"
        app:layout_constraintStart_toEndOf="@id/tv_passenger_id_label"
        app:layout_constraintTop_toBottomOf="@id/tv_passenger_name"
        tools:text="12345" />

</androidx.constraintlayout.widget.ConstraintLayout>